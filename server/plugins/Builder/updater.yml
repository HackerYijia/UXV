# Configuration for the Builder updater system
# it will inform you about new versions of all plugins which use this updater
# 'enabled' specifies whether the system is enabled (affects all plugins)
enabled: true
enabledingame: true
